export interface IFood {
  foodId: number;
  foodName: string;
  price: number;
}

// TODO include select button in search component
// TODO create a cart service to store the products that has been selected
// TODO  - create a cart service
// TODO  - define an array inside the cart to store the products
// TODO  - define (addItem), (getItem), (removeItem), (clearCart) methods in the cart
// TODO  - bind the click event of the button to the addToCart method
// TODO  - use the product as argument to the addItem method


// TODO create a CART component (checkout) page, and include it in the menu along with SEARCH and LOGIN and routing
// TODO  - display de cart contents


