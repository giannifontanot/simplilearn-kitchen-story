export interface IFood {
  foodId: number;
  foodName: string;
  price: number;
}

/* TODO - login page and checkout
    - write the form in the html
      - name, address, and button
    - - property binding - formGroup, event submit ngSubmit

    - in the component,
    - - define the group from formBuilder: checkoutForm
    - - - use name and address
    - - - define what happens with onSubmit()

*/
